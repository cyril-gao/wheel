package com.example;

public interface SubstringSearcher {
    /*
     * The method returns the index within the String object s of the first
     * occurrence of the specified value p. Returns -1 if the value is not found.
     */
    int search(String s, String p);
}
